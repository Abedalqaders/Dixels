version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=SpaceOs_Identity
      - SpaceOs_DB=SpaceOs_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
